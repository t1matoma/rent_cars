# Rent Cars API

Система аренды автомобилей с FastAPI и PostgreSQL.
Позволяет пользователям регистрироваться, бронировать машины, проверять доступность, а администраторам управлять автопарком и бронированиями.

## 🛠 Технологии

Python 3.13

FastAPI

SQLAlchemy

PostgreSQL

Docker / Docker Compose

bcrypt для хеширования паролей

## 📦 Установка и запуск
```bash
git https://github.com/t1matoma/rent_cars
cd rent_cars

```

## Собираем и запускаем проект:

```bash
docker-compose up --build
```


FastAPI будет доступен на: `http://localhost:8000`

Документация Swagger: `http://localhost:8000/docs`

## ⚙️ Использование API
Аутентификация

POST /auth/register — регистрация пользователя

POST /auth/login — логин, возвращает user_id

Пользовательские действия

POST /user/{id}/book — бронирование машины

GET /cars — список машин и их доступность

Администратор

POST /admin/cars — добавить машину

PUT /admin/cars/{id} — редактировать машину

DELETE /admin/cars/{id} — удалить машину

GET /admin/bookings — список всех бронирований


## 📌 Команды разработки

Перезапуск контейнеров:

docker-compose down
docker-compose up --build
